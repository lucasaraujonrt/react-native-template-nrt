import { DefaultTheme } from 'styled-components/native';

const theme: DefaultTheme = {
  tab: {
    active: '#66519E',
    background: '#fff',
  },
  colors: {
    primary: '#66519E',
    background: '#fff',
    text: '#000',
  },
};

export default theme;
